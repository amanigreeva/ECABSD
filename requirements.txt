# ECABSD — pinned requirements
# Install order matters for PyG. Follow the README installation steps exactly:
#
#   pip install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 \
#       --index-url https://download.pytorch.org/whl/cpu
#
#   pip install torch-geometric==2.7.0
#
#   pip install torch-scatter torch-sparse torch-cluster \
#       --find-links https://data.pyg.org/whl/torch-2.1.0+cpu.html
#
#   pip install -r requirements.txt

# --- Web / API ---
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.20

# --- Core ML (versions must match the PyTorch wheel above) ---
torch==2.1.0
torch-geometric==2.7.0

# --- Biology ---
biopython==1.87
pydssp==0.9.1

# --- Numerics / utilities ---
numpy>=1.24.0,<2.0.0
scipy==1.12.0
PyYAML==6.0.3
scikit-learn==1.4.2
tqdm==4.66.4
psutil==5.9.8

# --- Visualisation ---
matplotlib==3.10.3
seaborn==0.13.2

# --- NLP (ESM-2 embeddings) ---
transformers==4.40.2

# --- CLI ---
typer==0.12.3
